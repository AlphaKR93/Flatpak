id: com.jetbrains.PyCharm
default-branch: beta
command: pycharm
finish-args:
  # Subsystems shared with the host
  - --share=network
  - --share=ipc

  # Socket access
  - --socket=x11
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --socket=ssh-auth

  # Device access
  - --device=dri
  - --device=kvm

  # Allowed Features
  - --allow=devel
  - --allow=multiarch

  # Allowed FS subsets
  - --filesystem=host
  - --filesystem=xdg-run/app/com.discordapp.Discord:create
  - --filesystem=xdg-run/docker
  - --filesystem=xdg-run/gnupg:ro
  - --filesystem=xdg-run/keyring
  - --filesystem=xdg-run/podman

  # Persistent dirs
  # - --filesystem=.java

  # Environment variables
  - --env=PYCHARM_PROPERTIES=${FLATPAK_DEST}/bin/idea.properties
  - --env=PYCHARM_JDK=${FLATPAK_DEST}/extra/jbr/

  # Bus
  - --talk-name=com.canonical.AppMenu.Registrar
  - --talk-name=org.freedesktop.Flatpak
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.freedesktop.secrets
add-extensions:
  com.jetbrains.PyCharm.Extensions:
    directory: extensions
    version: '23.08'
    add-ld-path: lib
    merge-dirs: bin;lib
    subdirectories: true
    no-autodownload: true
modules:
  - shared-modules/libsecret/libsecret.json
  - src/python3-virtualenv.yaml
  - src/python3-pipenv.yaml
  - src/python3-poetry.yaml

  - name: rsync
    cleanup:
      - /share/man
    config-opts:
      - --prefix=${FLATPAK_DEST}
      - --with-included-popt
      - --with-included-zlib
      - --disable-debug
      - --disable-md2man
      - --disable-xxhash
    sources:
      - type: archive
        url: https://download.samba.org/pub/rsync/src/rsync-3.4.1.tar.gz
        sha256: 2924bcb3a1ed8b551fc101f740b9f0fe0a202b115027647cf69850d65fd88c52
        x-checker-data:
          type: anitya
          project-id: 4217
          stable-only: true
          url-template: https://download.samba.org/pub/rsync/src/rsync-$version.tar.gz

  - name: wrapper
    buildsystem: meson
    config-opts:
      - -Deditor_binary=/app/extra/PYCHARM/bin/pycharm.sh
      - -Deditor_title=PyCharm Professional
      - -Dprogram_name=pycharm
    sources:
      - type: git
        commit: 5a054811ab2144ceaf343169b240a70eeb4dfb47
        url: https://github.com/Lctrs/jetbrains-flatpak-wrapper.git

  - name: idea
    build-commands:
      - install -d "${FLATPAK_DEST}/packages"
      - install -d "${FLATPAK_DEST}/extensions"
      - install -Dm 644 -t ${FLATPAK_DEST}/bin idea.properties

      # DO NOT EDIT
      - install -Dm 755 apply_extra ${FLATPAK_DEST}/bin/apply_extra
      - install -Dm 644 -t ${FLATPAK_DEST}/share/metainfo ${FLATPAK_ID}.metainfo.xml
      - install -Dm 644 -t ${FLATPAK_DEST}/share/applications ${FLATPAK_ID}.desktop
      - install -Dm 644 -t ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps ${FLATPAK_ID}.svg
    buildsystem: simple
    sources:
      - type: file
        path: com.jetbrains.PyCharm.metainfo.xml

      - type: file
        url: https://resources.jetbrains.com/storage/products/company/brand/logos/PyCharm_icon.svg
        sha256: 9ffd475c5139293ac050ea01edeea1e48b0412840e30c54a9238039465a03275
        size: 45166
        dest-filename: com.jetbrains.PyCharm.svg

      - commands:
          - mkdir PYCHARM/
          - tar -xzf source.tar.gz --directory=PYCHARM/ --strip-components=1
          - rm source.tar.gz
          - python3 /app/extra/plugins/python-ce/helpers/pydev/setup_cython.py build_ext --inplace

        # DO NOT EDIT
        dest-filename: apply_extra
        type: script

      - x-checker-data:
          type: jetbrains
          code: PCP
          release-type: eap
          is-main-source: true
        only-arches:
          - x86_64

        url: https://download.jetbrains.com/python/pycharm-253.28086.55.tar.gz
        sha256: 32df557617b2e5d4462303a49f977f529341fbb7e08b99f32121b281db1cb846
        size: 1197805892

        # DO NOT EDIT
        type: extra-data
        filename: source.tar.gz

      - x-checker-data:
          type: jetbrains
          code: PCP
          release-type: eap
        only-arches:
          - aarch64

        url: https://download.jetbrains.com/python/pycharm-253.28086.55-aarch64.tar.gz
        sha256: f5e3e509ccbc6d7e1550afad8540953d7f5810d080563f22a8050cda3cfcba80
        size: 1193463455

        # DO NOT EDIT
        type: extra-data
        filename: source.tar.gz

      # DO NOT EDIT
      - type: dir
        path: src

sdk: org.freedesktop.Sdk
tags: [proprietary]
runtime: org.freedesktop.Platform
runtime-version: '23.08'
separate-locales: false
