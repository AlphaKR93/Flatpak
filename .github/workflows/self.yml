name: Build and publish Flatpak package manually
on:
  push:
  workflow_dispatch:

jobs:
  package:
    uses: ./.github/workflows/build.yml
    secrets:
      GPGPK: ${{ secrets.GPGPK }}
      GPGPW: ${{ secrets.GPGPW }}
      GHPAT: ${{ secrets.GHPAT }}
